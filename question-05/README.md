# Registration App

[Return to main page](../README.md)

To run unit tests:

```sh
npm test
```

To run e2e tests on the command line:

```sh
npx cypress run
```

To run e2e tests in the browser:

```sh
npx cypress open
```
